#include <bits/stdc++.h>
using namespace std;
void prime(int a,int b)
{
    
    int i,count;
    for(;a<=b;a++)
    {
        count=0;
        for(i=2;i<=a/2;i++)
        {
            if(a%i==0)
            {
                count++;
            }
            
        }
        if(count==0)
        { cout<<a<<" ";}
    }
}
int main()
{
    int p,q;
    cout<<"enter two numbers";
    cin>>p>>q;
    cout<<"prime numbers are : ";
    prime(p,q);
    return 0;
}
