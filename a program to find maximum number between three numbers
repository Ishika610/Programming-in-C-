#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, max;
    cout << "enter two numbers";
    cin >> a >> b >> c;
    max = a > b && a > c ? a : (b > c ? b : c) ;
    cout << max;
    return 0;
}
