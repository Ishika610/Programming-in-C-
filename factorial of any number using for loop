#include <bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
    int n,i,fact=1;
    cout<<"enter a number ";
    cin>>n;
    for ( i = n; i >=1; i--)
    {
        fact=fact*i;
    }
    cout<<"factorial is "<<fact;
    return 0;
}
